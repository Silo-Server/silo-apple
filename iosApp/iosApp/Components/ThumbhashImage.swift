import SwiftUI

/// Placeholder for ThumbHash image decoding.
/// A future version will decode the thumbhash string into a blurred preview image.
struct ThumbhashImage: View {
    let thumbhash: String?

    var body: some View {
        Color.siloSurfaceVariant
    }
}
