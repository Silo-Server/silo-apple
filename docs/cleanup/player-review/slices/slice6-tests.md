# Slice 6 — Tests / observability / migration (HEAD 36393b4) — KEY FINDINGS

## Test inventory: 60 player test files ~18,225 LOC. P=27 files ~11,455 (63%); I=25 ~3,931 (22%); D=2 ~1,178 (6%) (ApplePlaybackRoutePlannerPinTests, PlayerErrorClassifierPinTests); X=6 ~1,661 (9%).
Pin tests self-described characterization (RoutePlannerPinTests:4-14). "PIN: likely bug": av01 not folded to av1 (:720-723); MKV CodecIDs A_EAC3/A_AC3/A_TRUEHD not folded → fall off native-direct audio allowlist (:761-764); FileVersion.bitrate ×1000 as if kbps (:910-912); classifier ordering (ErrorClassifierPin:40-43); "mux" matches "demuxer" (:161-163); "decoder failed"→decode vs "decoding failed"→playback_error (:174-186). Surprising-but-intended: DV in mp4 takes loopback ahead of native-direct (:384-387); lossless FLAC/PCM "transcoded" (:844).
Option C survival: proxy is NOT loopback-only — prepareSourceProxy gates on delivery == .direct && engine != .avPlayerHLS (PVM:2846-2848), sourceCacheBudget has .avPlayerNativeDirect branch (:2984-3001). Survive ~13,700 LOC/41 files; deleted ~4,000 LOC/17 files (Loopback*×14, ISOBox*×3, LocalHLS*×2, HDR10PlusSEI, TrueHDPriming, TrueHDAudioPipeline, GeneratedAhead, PlaybackMediaFixture + fixtures); rewritten: RoutePlannerPin, HDRDisplayCriteriaPolicyTests, DolbyVisionPolicyTests.

## Orchestration gap: ZERO tests construct PlayerViewModel / AVPlayerBackend / PlaybackSessionBridge; no `AVPlayer(` in tests; no refs to PiP/AirPlay/NowPlaying/SleepTimer/RealtimeClient/TaskRegistry/PlaybackStats. attemptNativeDirectRouteRecovery PVM:2124-2203 hand-snapshots ~14 state pieces across resetPublishedLoadState (:2181-2186), untested.
Seams: ApplePlaybackRoutePlanner pure struct (:194,:248); V3PlanAdapter.makeExecutionPlan pure (:289); PlayerSettingsTransport/WriteJournal protocols + FakeSettingsTransport (PlayerSettingsFlusher.swift:38,:149); LivePlaybackControls/LiveSubtitleSink/... (LiveSubtitleCoordinator.swift:54,65,110,118) — only well-seamed orchestrator (400 LOC/19 tests); PlaybackOriginStreamClock (PlaybackSourceOriginStream.swift:271); PlayerSettings(defaults:transport:) (:322-326).
Hard-wired: no backend protocol (7 protocols in Player, none for backend); final class AVPlayerBackend init() no params (:380,:763); let avPlayer = AVPlayer() :571; AVPlayerItem inline :1774,:2184,:3689; VM constructs backend :1118-1119,:1130; bridge actor calls SiloAPI.shared 19×; VM 8 singletons; loadAndPlay returns Void, detached Task :4450-4473.

## Observability: two disjoint channels. cmpLog (PlayerLog.swift:5) → DiagLog.i(.playback,"CMP") :37 is ONLY diagnostics path; cmpLog used 1× in PVM (:2365 logExecutionPlan) vs 106 Self.logger sites → recovery/outage/seek/lifecycle lines never reach crash bundles. Repo: ~137 cmpLog (73 writer, 49 backend) vs ~250 OSLog. [CMP-ROUTE] plan line :2330-2366 excellent (fallbackTrail=decisionTrace; planner :379-387,:423; appends :2280,:2957,:5086). [CMP-RECOVERY] 13 lines (:3287,4001,...); [CMP-OUTAGE] 5; [CMP-SEEK] 19; [CMP-ENGINE] :1134,:1148; [CMP-LIFECYCLE] 5; [CMP-SUB] 11; no audio-track tag. PlaybackStats 78 fields (:7-84), zero tests. Correlation: playbackSessionId in exactly 1 VM log line (:2348); streamLoadGeneration never logged (:2696). Requirements: typed event log per load with LoadIdentity; one channel; in-process assertable ring; rung/attempt counters on stats.

## Flags: six player.apple.* all local UserDefaults debug switches, no remote route gate (last retired 2026-08-17 docs/05:40): hdr_display_criteria_enabled (default true; HDRDisplayCriteriaPolicy:19-24), loopback_mux_qos_boost (writer:499-506), loopback_master_start_enabled (:585-591), loopback_bridged_drift_correction_enabled (:833-836), loopback_progressive_anchor_enabled (:6001-6004), loopback_trace_throughput (:375-377, opt-in). Remote (SettingKeys.generated.swift:71-97; PlayerSettings:339-370): player.dolby_vision_enabled (ONLY remote setting changing route; planner:259,:256-268), dv_profile7_hdr10_fallback, hdr_enabled (removed effect), seek_cache_enabled (writer behavior PVM:2864-2872), others presentation. Migration needs NEW remote key via same pattern (flusher.enqueue :538-554).

## Migration
Stage 0 tests writable today (15): V3 decision→plan per fixture (adapter:289); decisionTrace snapshot ~12 inputs (planner:248); logExecutionPlan string (needs static); needsServerReplanBeforeLoad (PVM:2384); makeLoopbackFallbackPlan nil/non-nil (:2247+); classifier ×30; stablePlaybackFailureToken; replanOperation total (bridge:899); canRetargetDirectSession (:864); isMaterialOutputRouteChange (:914); HDRDisplayCriteriaPolicy.selection; proxy eligibility predicate (:2846-2858); sourceCacheBudget (:2984-3001); OfflineManifest→plan; V3 fixture round-trip. NOT writable: "exactly one fallback per failure" (private Bool :845, Void entry).
Stage 1: protocol PlaybackBackend at makeAVPlayerBackend :1118-1119; inject AVPlayer :571. Stage 2: extract PlaybackController behind remote key player.apple.control_plane_v2 string "off|native_direct|loopback|all". Stage 3 validate → default on. Stage 4 delete.
Option B: drop bridge tier first (siloBridgeSourceContainers 13 + siloVideoBridgeCodecs 9, planner:210-227 → one line each; delete LoopbackVideoBridge.swift 810 + tests + fixtures); then non-DV copy tier; keep TrueHD/DV until validated. Option C: AVPlayerRoute/ 16 files minus AVPlayerBackend/AVPlayerSurface ≈11,600 LOC + PlaybackEngineKind.siloPlayerLoopback (:395), routeFamily.siloPlayer (:350), planner allowlists :200-244, assessSiloRoute/blockedSilo/makeLoopbackSessionSpec/loopbackVideoOutputMode/loopbackAudioOutputMode (:461,590,667,766,928); DO NOT delete proxy/origin stream (serve native-direct). Rollback for C = revert+TestFlight.

## Hardware matrix: docs/tvos-player/validations/ has only README — ZERO records though docs/05:40,:5 claim DV P8+EAC3 (2026-07-03) and AirPlay (2026-07-24) validated. 14 rows: (1) ATV DV P7 MKV+TrueHD/Atmos loopback (capture -17223 baseline); (2) DV P5 mp4; (3) HDR10 MKV flag on/off; (4) h264 mp4 native-direct; (5) PGS+ASS; (6) DVB → HLS; (7) 4K seek-heavy; (8) AirPlay loopback; (9) iPhone PiP native; (10) iPhone background/lock; (11) offline download unproxied airplane mode; (12) source expiry 404 → renewal; (13) server outage ride-through; (14) fallback ladder end-to-end exactly-one-rung. Core rows 1,3,7,14.
